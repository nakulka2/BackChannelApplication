<%= form_for(@reply) do |f| %>
  <% if @reply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reply.errors.count, "error") %> prohibited this reply from being saved:</h2>

      <ul>
      <% @reply.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :response %><br />
    <%= f.text_area :response %>
    <%=hidden_field_tag 'post_rid', params[:post_pid] %>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
